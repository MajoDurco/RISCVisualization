// import React from 'react';
// import { shallow } from 'enzyme';

// import JumpTresholdExceeded from '../index';

describe('<JumpTresholdExceeded />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
